# .NET
bin/
obj/
*.user
*.suo
[Bb]uild/
[Oo]ut/
artifacts/
TestResults/
*.coverage
*.trx
# La carte de couverture (ADR 0029), produite par `mise run coverage`
backend/coverage/

# Front (le Client généré est ignoré par frontend/.gitignore)
node_modules/
dist/
.vite/

# IDE
.idea/
.vs/
.vscode/*
!.vscode/extensions.json

# OS
.DS_Store

# Secrets locaux
.env
*.env.local

# Brouillons des agents (les issues vivent sur GitHub)
.scratch/

# Agents — les hooks versionnés vivent dans .claude/settings.json (ADR 0033),
# son jumeau local n'a rien à faire ici, et l'horodatage que lit `hook:verify`
# est une trace de poste.
.claude/settings.local.json
.claude/.gates-ran
